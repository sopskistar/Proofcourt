"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { ChevronDown, CircleCheck, CircleX, Scale, ShieldAlert } from "lucide-react";
import { claimApi } from "@/services/claims";
import type { Claim } from "@/types/claims";
import { dateTime, money } from "@/lib/format";
const icons = { APPROVED: CircleCheck, DENIED: CircleX, ESCALATED: ShieldAlert };
export function VerdictScreen({ claimId }: { claimId: string }) { const [claim, setClaim] = useState<Claim>(); const [error, setError] = useState(""); const [open, setOpen] = useState(false); useEffect(() => { claimApi.get(claimId).then(setClaim).catch(e => setError(e.message || "We couldn’t load this verdict.")); }, [claimId]); if (error) return <section className="container narrow state"><ShieldAlert /><h1>Verdict unavailable</h1><p>{error}</p><Link className="button" href={`/review/${claimId}`}>Return to review</Link></section>; if (!claim || !claim.verdict) return <section className="container narrow state"><Scale className="spin" /><h1>Verdict not ready</h1><p>The backend has not reported an authoritative final verdict yet.</p><Link className="button" href={`/review/${claimId}`}>Continue monitoring</Link></section>; const v = claim.verdict; const Icon = icons[v.verdict]; return <section className="container verdict"><div className={`verdict-hero ${v.verdict.toLowerCase()}`}><Icon size={38} /><div><span>Authoritative adjudication</span><h1>{v.verdict}</h1><p>{v.reasoningSummary || "The adjudication was finalized by the backend’s GenLayer workflow."}</p></div></div><div className="verdict-grid"><section className="card"><h2>Decision summary</h2><dl><dt>Claim ID</dt><dd>{claim.id}</dd><dt>Confidence</dt><dd>{v.confidence != null ? `${v.confidence}%` : "Not provided"}</dd><dt>Evidence</dt><dd>{claim.evidence?.length || 0} item(s)</dd><dt>Covered event</dt><dd>{v.coveredEvent == null ? "Not provided" : v.coveredEvent ? "Yes" : "No"}</dd><dt>Loss supported</dt><dd>{v.lossSupported == null ? "Not provided" : v.lossSupported ? "Yes" : "No"}</dd><dt>Policy match</dt><dd>{v.policyMatch == null ? "Not provided" : v.policyMatch ? "Yes" : "No"}</dd></dl>{v.recommendedPayout != null && <p className="payout"><span>Recommended Payout</span><strong>{money(v.recommendedPayout, claim.currency)}</strong></p>}{v.recommendedAction && <p><strong>Recommended next action</strong><br />{v.recommendedAction}</p>}</section><section className="card"><h2>Evidence summary</h2>{claim.evidence?.length ? <ul className="evidence-list">{claim.evidence.map((item, i) => <li key={item.id}><span className="evidence-number">{i + 1}</span><div><strong>{item.filename}</strong><span>{item.hash ? `Verified hash: ${item.hash}` : "Hash not supplied"}</span></div></li>)}</ul> : <p>No evidence metadata was supplied.</p>}</section></div><section className="details"><button aria-expanded={open} onClick={() => setOpen(!open)}>Technical details <ChevronDown size={18} className={open ? "rotate" : ""} /></button>{open && <dl><dt>Evidence hash</dt><dd>{claim.evidence?.map(x => x.hash).filter(Boolean).join(", ") || "Not available"}</dd><dt>GenLayer transaction</dt><dd>{claim.adjudication?.genlayerTransactionId || "Not available"}</dd><dt>EVM transaction</dt><dd>{claim.adjudication?.evmTransactionId || "Not available"}</dd><dt>Contract</dt><dd>{claim.adjudication?.contractAddress || "Not available"}</dd><dt>Network</dt><dd>{claim.adjudication?.network || "Not available"}</dd><dt>Finalization status</dt><dd>{claim.adjudication?.finalizationStatus || claim.status}</dd><dt>Timestamp</dt><dd>{dateTime(v.timestamp || claim.adjudication?.finalizedAt)}</dd></dl>}</section></section>; }
